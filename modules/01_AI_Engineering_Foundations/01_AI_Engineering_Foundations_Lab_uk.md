# Модуль 01: Основи проєктування систем штучного інтелекту — Лабораторна робота

> **Статус:** Ready for Students — англомовну лабораторну роботу затверджено 2026-09-06 як частину англомовної пари модуля 01

## Мета

Ініціалізувати й перевірити робочий контур для наданої наскрізної навчальної системи знань: отримати проєкт через обов’язковий шлях власності в Git, відтворити підтримуване середовище робочої станції, створити належні студентові ділянки проєкту та зовнішнє сховище Markdown-файлів, зареєструвати перше джерело курсу та зберегти свідчення результату. Далі обмежена, зазвичай згенерована ШІ пропозиція щодо межі системи перевіряє поділ повноважень, не надаючи агентові повноважень прийняти власну пропозицію. Якщо жоден придатний агент недоступний, задокументований ручний запасний шлях зберігає межі кандидата, перевірки, рішення та застосування без фабрикації сеансу агента; він не демонструє живого розділення між акторами ШІ та студента.

Завершена лабораторна робота дає п’ять спостережуваних результатів. Середовище та ініціалізований стан проєкту є головним результатом; керована пропозиція є обмеженою діяльністю перевірки всередині цього результату:

- студентський форк публічного репозиторію курсу на GitHub, у якому `origin` вказує на цей форк, `upstream` — на репозиторій викладача, а виконання лабораторної роботи обмежене шляхами, що належать студентові;
- підтримувана робоча станція, на якій тести проєкту та потрібні засоби командного рядка виконуються відтворювано;
- створені студентом шляхи `student/design/` та `reports/lab01/`, що містять потрібні свідчення проєктування, звіту, журналу, машинозчитувані свідчення та знімки екрана;
- зовнішнє сховище Markdown-файлів, що належить студентові, яке містить файл орієнтації та зареєстроване джерело теорії модуля 01, межу якого та початковий захист від утрати робочої станції можна пояснити;
- прийнятий контракт `learning-system-boundary.yaml`, походження, людське затвердження та застосування якого можна перевірити.

## Очікувані компетентності

Після виконання лабораторної роботи студент здатний:

- розрізняти демонстрацію працюючої моделі та контрольований робочий процес системи ШІ;
- отримувати файли курсу з особистого форку, а не з вкладень викладача;
- розрізняти `origin` і `upstream` та розрізняти шляхи, що належать `upstream`, і артефакти, що належать студентові;
- відтворювати потрібний набір інструментів і перевіряти його повторним застосуванням або повторною перевіркою можливостей;
- розрізняти відтворювану інфраструктуру, означення під контролем версій, змінний канонічний стан і похідні артефакти, які можна відбудувати;
- пояснювати надану навчальну систему знань, реєструвати посилання на теорію модуля 01, яке однозначно визначає версію, і зберігати можливу особисту сферу як пізніше обмежене розширення, а не як іншу систему;
- оцінювати надану межу системи через запланований результат, нецілі, умову корисності, істотний ризик і простішу базову лінію без ШІ;
- зберігати згенерований ШІ вміст як пропозицію, доки не відбудуться детермінована перевірка та явне людське затвердження;
- зберігати свідчення, не публікуючи облікові дані, чутливий або сторонній вміст сховища чи сторонні дані робочої станції.

## Передумови

Цю лабораторну роботу виконати самостійно до запланованого заняття. Заняття відведено для демонстрації результату, відповідей на контрольні запитання та обговорення проєктних рішень.

Обов’язкові початкові умови на кожному підтримуваному хості такі:

- теорію модуля 01 вивчено;
- студент має обліковий запис GitHub;
- доступні текстовий редактор і веббраузер;
- студент може скористатися шляхом агента або задокументувати, чому жоден шлях не є придатним, і дотримуватися ручного запасного шляху. Типовим пропонувачем є Antigravity CLI, автентифікований обліковим записом Google; студент, який уже має іншу підписку на агента, може використати її замість цього.

Windows 11 є основним задокументованим шляхом робочої станції та хостом більшості студентів. Студент на Windows має бути здатним затверджувати запити адміністратора, а WinGet має бути доступним через Windows App Installer.

Linux є рівноцінним шляхом для тієї самої межі репозиторію, керованого робочого процесу пропозиції та перевірок можливостей `learning-project doctor`. Це не друге видання курсу. Студент на Linux має бути здатним установити Git, GitHub CLI, `uv` та Obsidian за допомогою менеджера пакунків дистрибутива або офіційного інсталятора кожного засобу.

macOS не є задокументованим шляхом у цьому виданні. Хост лише з macOS є червоним результатом передпольотної перевірки: зупинитися й повідомити про це замість імпровізації третього робочого процесу.

Публічний репозиторій курсу — https://github.com/sobol-mo/ai-systems-design-course. Кожен файл лабораторної роботи надходить із клону студентського форку цього репозиторію. Викладач не додає `dsc.yaml`, стартову пропозицію чи інші файли проєкту поза Git.

Обов’язковий шлях не потребує платного API моделі, платіжної картки, середовища виконання контейнерів, віртуальної машини чи Windows Subsystem for Linux. Робоча станція Linux уже є підтримуваним хостом і не потребує WSL. Antigravity CLI використовує квоту, доступну автентифікованому обліковому запису. Інша наявна підписка на агента використовує квоту цієї підписки. Якщо жоден придатний агент не залишається, скористатися задокументованим шляхом ручної пропозиції та чесно зафіксувати обмеження; не купувати доступ до API лише заради цієї лабораторної роботи.

## Початковий стан

Студент починає з порожньої робочої теки та облікового запису GitHub. Після кроку 2 локальний клон є єдиним джерелом файлів лабораторної роботи. Конфігурація робочої станції — `training-project/fixtures/windows/lab01-workstation.dsc.yaml`. Стартова пропозиція — `training-project/boundary-proposal.yaml`. Обидва файли належать `upstream`: студент використовує їх із клону й не редагує копії `upstream`.

Той самий клон постачає означення системи в `training-project/requirements/SYSTEM_BRIEF.md`, зобов’язальні вимоги в `training-project/requirements/REQUIREMENTS_BASELINE.md` та правила організації зовнішнього сховища в `training-project/requirements/VAULT_STRUCTURE.md`. Ці файли визначають систему, яку будує кожен студент. Вони є вхідними даними проєкту для читання та застосування, а не матеріалом для з’ясування вимог, заміни чи винайдення архітектури.

Конфігурація робочої станції Windows керує Git, GitHub CLI, `uv` та Obsidian. Linux установлює ті самі чотири можливості без застосування цього файлу. Antigravity CLI є типовим пропонувачем і встановлюється окремо своїм офіційним інсталятором, бо автентифікація та змінний стан агента залишаються у власності студента. Інший агент конфігурацією курсу не встановлюється; студент автентифікує наявне середовище взаємодії з ШІ-агентом і спрямовує його на той самий файл пропозиції.

Нумеровані кроки показують Windows PowerShell, бо це основний задокументований шлях. Linux замінює команди POSIX для каталогів і файлів (`mkdir -p`, `cp`, `rm`) та шляхи POSIX (`reports/lab01/...` замість `.\\reports\\lab01\\`). Команди Git, `uv` та `learning-project` в іншому однакові. Не перетворювати інсталятор лише для Windows на нерозглянуту команду Linux.

Технічну базову лінію перевірено на Windows 11 25H2 із WinGet Configuration 1.29, `uv` 0.12.9 та Antigravity CLI 1.1.26. Linux приймається на рівні можливостей: `learning-project doctor` має повідомити зелений хост Linux із доступними Git, GitHub CLI, `uv` та Obsidian. Пізніший сумісний випуск прийнятний, коли команди та перевірки прийняття в цій лабораторній роботі й надалі поводяться як задокументовано. Тому конфігурація Windows оголошує ідентичності пакунків, а не недоступні історичні двійкові файли, і проєкт перевіряє можливості після встановлення. Це дає відтворюваність на рівні можливостей, а не побітово точний образ робочої станції. Зафіксувати кожну спостережену версію та фактично використаного пропонувача в `REPORT.md`; не замінювати мовчки невдалу команду іншим робочим процесом.

Клон є одним репозиторієм Git із двома віддаленими репозиторіями. Теки не є віддаленими репозиторіями. `origin` — це форк студента на GitHub і єдиний віддалений репозиторій, який отримує `git push`. `upstream` — це публічний репозиторій курсу https://github.com/sobol-mo/ai-systems-design-course, і його використовують лише для отримання пізніших публікацій викладача. Студент ніколи не надсилає зміни до `upstream`.

Наведене далі дерево є опублікованою структурою курсу, яку має містити клон. Шляхи, позначені **upstream**, підтримує курс, і їх можуть додавати, замінювати або вилучати в пізнішій публікації; локальне редагування на цих шляхах може бути перезаписане під час наступної синхронізації з `upstream`. Шляхи, позначені **student**, не існують, доки їх не створить студент; це єдині шляхи проєкту, які студент фіксує комітом і надсилає до `origin`. Шляхи, позначені **derived**, є похідним машинним станом, який можна відбудувати, і їх не слід фіксувати комітом.

```text
ai-systems-design-course/                  clone root
  README.md                                upstream — course navigation
  00_Curriculum.md                         upstream
  GLOSSARY.md                              upstream
  references/                              upstream
  modules/                                 upstream — theory and laboratory instructions
  training-project/                        working project
    README.md                              upstream
    pyproject.toml                         upstream
    uv.lock                                upstream
    .gitignore                             upstream
    requirements/                          upstream — supplied system definition and requirements
    boundary-proposal.yaml                 upstream starter — copy, do not edit in place
    fixtures/                              upstream — including lab01-workstation.dsc.yaml
    platform/                              upstream — CLI, doctor, and proposal workflow
    tests/public/                          upstream — public acceptance tests
    student/                               student — design and later implementation
    reports/                               student — laboratory evidence
    .venv/                                 derived — created by uv, ignored by Git
```

Зовнішнє сховище Markdown-файлів належить студентові й розташоване **поза** цим деревом. Obsidian є наданим інтерфейсом для його відкриття, але канонічний стан залишається звичайними Markdown-файлами. Сховище не є частиною репозиторію Git і не надсилається до `origin`.

Лабораторна робота розрізняє п’ять розташувань стану з різними повноваженнями та власністю:

| Розташування стану | Уповноважений засіб запису | Значення |
|---|---|---|
| `reports/lab01/boundary-proposal.yaml` | ШІ на звичайному шляху; студент у разі виправлення або використання ручного запасного шляху | Вміст кандидата без повноважень змінювати прийнятий проєкт |
| `reports/lab01/boundary-decision.json` | `learning-project decide` | Свідчення рішення та аудиту; CLI проєкту відмовляється перезаписувати зафіксований результат |
| `student/design/learning-system-boundary.yaml` | `learning-project apply` | Прийнятий контракт межі системи, згенерований лише із затвердженої пропозиції; не еталонна архітектура |
| Зовнішнє сховище Markdown-файлів | Студент під час Лабораторної роботи 01; керований процес у пізніших модулях | Канонічне навчальне знання, а не стан проєктування проєкту |
| Перелічені вище шляхи, що належать `upstream` | Робочий процес публікації курсу | Надані контракти, матеріали курсу, CLI проєкту, фікстури та тести, які студент читає й запускає, але не редагує |

Не редагувати `boundary-decision.json` або прийнятий контракт вручну. Якщо зафіксоване рішення має змінитися, зберегти наявні свідчення, вилучити згенероване рішення та прийнятий контракт, переглянути пропозицію під новим `proposal_id` і виконати новий цикл розгляду.

## Кроки

### Крок 1: Виконати передпольотну перевірку лише для читання

На Windows відкрити PowerShell без підвищення прав адміністратора. Не встановлювати пакунки й ще не клонувати. Виконати такі перевірки:

```powershell
$PSVersionTable.PSVersion
[Environment]::OSVersion.Version
winget --version
winget configure --help
```

На Linux замість WinGet виконати перевірку хоста лише для читання:

```bash
uname -s
cat /etc/os-release
git --version
```

Класифікувати результат до зміни робочої станції:

- **зелений (Windows):** доступні Windows 11, WinGet і `winget configure`;
- **зелений (Linux):** `uname -s` повідомляє `Linux`; Git ще може бути відсутнім до початкового встановлення на кроці 2;
- **жовтий (Windows):** WinGet існує, але його треба оновити через Microsoft Store, перш ніж можна застосувати конфігурацію;
- **червоний:** macOS, Windows старіша за 11, недоступний Windows App Installer, відсутнє затвердження адміністратора на Windows або хост Linux, на якому студент не може встановити потрібні засоби.

Усунути жовтий результат до продовження. Зупинитися й повідомити про червоний результат замість заміни підтримуваного шляху нерозглянутими командами встановлення.

**Очікуваний результат:** хост Windows повідомляє версію `10.0.22000` або новішу, і обидві команди WinGet показують версію або довідку, не змінюючи встановлених пакунків. Хост Linux повідомляє `Linux` і не встановлює пакунки під час передпольотної перевірки.

### Крок 2: Створити форк, клонувати та прив’язати віддалені репозиторії

Кожен наступний крок читає файли з цього клону. Тому форк, два віддалені репозиторії та особиста гілка є частиною цієї лабораторної роботи, а не окремою вправою з Git.

Git потрібен до клонування. Перевірити, чи він уже наявний:

```powershell
git --version
```

Якщо Git відсутній на Windows, відкрити PowerShell **від імені адміністратора** і встановити лише цей початковий пакунок:

```powershell
winget install --id Git.Git -e --source winget --accept-package-agreements --accept-source-agreements
```

Якщо Git відсутній на Linux, установити його менеджером пакунків дистрибутива. Не застосовувати файл конфігурації WinGet для Windows для цього початкового встановлення.

На Windows закрити всі вікна PowerShell і відкрити нове вікно без прав адміністратора, щоб завантажився оновлений `PATH`. Знову підтвердити `git --version`.

Один раз зафіксувати ідентичність Git на цій робочій станції. Ім’я та адреса електронної пошти з’являються в кожному коміті:

```powershell
git config --global user.name "<student-name>"
git config --global user.email "<github-email>"
git config --global user.name
git config --global user.email
```

Створити форк у браузері, а не в терміналі. Відкрити https://github.com/sobol-mo/ai-systems-design-course, вибрати **Fork** і створити копію під обліковим записом студента на GitHub. Зберегти назву репозиторію `ai-systems-design-course`. Форк — це окремий репозиторій GitHub, до якого студент може надсилати зміни. Це ще не тека на робочій станції.

Клонувати **форк**, а не репозиторій викладача:

```powershell
New-Item -ItemType Directory -Force "$HOME\projects" | Out-Null
Set-Location "$HOME\projects"
git clone "https://github.com/<GITHUB-LOGIN>/ai-systems-design-course.git"
Set-Location .\ai-systems-design-course
git remote -v
```

Замінити `<GITHUB-LOGIN>` на ім’я облікового запису GitHub. Linux використовує `mkdir -p "$HOME/projects"` і `cd` замість `New-Item` і `Set-Location`; команди `git clone`, віддалених репозиторіїв і гілок ті самі. Після цього клону Git називає цей форк `origin`. Вивід має показувати обліковий запис студента, а не `sobol-mo`. Якщо `origin` вказує на `sobol-mo/ai-systems-design-course`, клоновано неправильний репозиторій: вилучити ту теку й клонувати форк.

Додати репозиторій викладача як `upstream`, щоб пізніше можна було отримувати публікації курсу. Потім створити особисту гілку лабораторної роботи. Не фіксувати комітом роботи лабораторної роботи на `main`.

```powershell
git remote add upstream https://github.com/sobol-mo/ai-systems-design-course.git
git remote -v
git switch -c lab01/<student-id>
git branch --show-current
```

Якщо `upstream` уже існує, перевірити його замість повторного додавання. Використати короткий інституційний ідентифікатор, який не розкриває зайвих персональних даних.

Обов’язкові віддалені репозиторії такі:

```text
origin    https://github.com/<GITHUB-LOGIN>/ai-systems-design-course.git (fetch)
origin    https://github.com/<GITHUB-LOGIN>/ai-systems-design-course.git (push)
upstream  https://github.com/sobol-mo/ai-systems-design-course.git (fetch)
upstream  https://github.com/sobol-mo/ai-systems-design-course.git (push)
```

`origin` — це особистий репозиторій студента на GitHub. Файли, що належать студентові, фіксуються комітом на особистій гілці й надсилаються лише до `origin`. `upstream` — це опублікований курс викладача. Теки, що належать `upstream`, оновлюються отриманням із `upstream`, а не редагуванням і не надсиланням змін до них.

Підтвердити, що клон містить `modules\01_AI_Engineering_Foundations\` і `training-project\fixtures\windows\lab01-workstation.dsc.yaml`. Пізніші кроки використовують ці шляхи. Їх не постачають як окремі вкладення.

Коли викладач пізніше в курсі публікує оновлення, синхронізувати `main` із `upstream`, а потім повернутися до особистої гілки. Не виконувати це злиття як заміну першого клонування і не зливати на брудну особисту гілку, що містить неопубліковані результати лабораторної роботи.

```powershell
git switch main
git fetch upstream
git merge upstream/main
git push origin main
git switch lab01/<student-id>
```

**Очікуваний результат:** `git remote -v` показує форк студента як `origin` і `sobol-mo/ai-systems-design-course` як `upstream`; `git branch --show-current` повідомляє `lab01/<student-id>`; `training-project\fixtures\windows\lab01-workstation.dsc.yaml` і `training-project\boundary-proposal.yaml` існують у клоні.

### Крок 3: Застосувати конфігурацію робочої станції двічі

На Windows відкрити PowerShell **від імені адміністратора**, перейти до каталогу `training-project` усередині клону та записати обидва запуски конфігурації в один транскрипт. Файл конфігурації — це копія з `upstream` у клоні. Студенти на Linux пропускають блок WinGet і використовують абзац для Linux після очікуваного результату для Windows.

```powershell
Set-Location "$HOME\projects\ai-systems-design-course\training-project"
Start-Transcript -Path .\provision.log -Force
winget configure --file .\fixtures\windows\lab01-workstation.dsc.yaml --accept-configuration-agreements
winget configure --file .\fixtures\windows\lab01-workstation.dsc.yaml --accept-configuration-agreements
winget list --id Git.Git --exact --source winget
winget list --id GitHub.cli --exact --source winget
winget list --id astral-sh.uv --exact --source winget
winget list --id Obsidian.Obsidian --exact --source winget
Stop-Transcript
```

Затверджувати лише угоди щодо конфігурації та джерела пакунків, показані для цього файлу. Не додавати сторонні пакунки до конфігурації лабораторної роботи. Не редагувати `fixtures/windows/lab01-workstation.dsc.yaml`.

Перше застосування узгоджує відсутні засоби. Друге застосування перевіряє збіжність: пакунки, які вже задовольняють оголошений стан, не повинні перевстановлюватися або знижуватися у версії. Зберегти `provision.log`; це обов’язкове свідчення. Переглянути його та вилучити облікові дані, секрети автентифікації та сторонній приватний матеріал перед поданням, але не переписувати результати конфігурації. Обліковий запис операційної системи або абсолютний шлях можуть залишатися, коли вони приписують свідчення робочій станції. Не фіксувати комітом `provision.log` у корені `training-project`; крок 10 копіює очищений від чутливих даних файл до `reports/lab01/`.

**Очікуваний результат:** усі чотири запити пакунків повертають установлений пакунок. Другий запуск конфігурації повідомляє, що оголошений стан пакунків уже задоволений, або завершується без перевстановлення чотирьох засобів.

Linux не застосовує `fixtures/windows/lab01-workstation.dsc.yaml`. Установити Git, GitHub CLI, `uv` та Obsidian менеджером пакунків дистрибутива або офіційним інсталятором кожного засобу, доки не будуть успішними `git --version`, `gh --version`, `uv --version` і сторінка About у Obsidian. Виконати три перевірки версій командного рядка вдруге; версії мають збігатися. Зберегти цей транскрипт термінала як `provision.log`. На Linux виконуваний файл `obsidian` має бути в `PATH`, щоб `learning-project doctor` міг його виявити. Не редагувати файл конфігурації Windows.

На Windows закрити всі вікна PowerShell і відкрити новий PowerShell без прав адміністратора, щоб завантажився оновлений `PATH`. На кожному підтримуваному хості перевіряти можливості, а не покладатися лише на назви пакунків:

```powershell
git --version
gh --version
uv --version
```

Запустити Obsidian один раз із меню Пуск на Windows або з елемента робочого столу чи встановленого двійкового файлу на Linux і зафіксувати версію, показану сторінкою About. Obsidian використовується як інтерфейс сховища Markdown-файлів; його запускач командного рядка не є вимогою прийняття. `learning-project doctor` усе одно має виявити застосунок.

### Крок 4: Підготувати типового пропонувача, іншого агента або ручний запасний шлях

Antigravity CLI є типовим пропонувачем цього року. Встановлювати його лише тоді, коли це середовище взаємодії з ШІ-агентом, яким студент користуватиметься. Студент, який уже має іншу підписку на агента, пропускає цей інсталятор, автентифікує те наявне середовище взаємодії з ШІ-агентом і використовує його на кроці 7 щодо того самого файлу пропозиції та тих самих шлюзів `validate` / `decide` / `apply`.

Шлях агента є **придатним**, коли наявний обліковий запис або підписка може відкрити локальний `training-project`, прочитати потрібні файли та завершити запитане редагування пропозиції в межах доступної квоти без нової покупки. Шлях непридатний, коли автентифікація, доступ до облікового запису, доступність служби або квота перешкоджають цій операції. Уподобання щодо засобу, зручність або небажання автентифікувати наявний обліковий запис не є недоступністю.

Якщо ні типовий пропонувач, ні інша наявна підписка на агента не є придатними, не купувати доступ до API і не фабрикувати транскрипт агента. Зберегти коротку примітку для пізнішого `REPORT.md` із назвою спробуваного шляху, датою та спостереженим результатом. Коли спроба дає помилку доступу, служби або квоти, зберегти очищений від чутливих даних транскрипт або знімок екрана цієї помилки. Якщо в студента немає наявного облікового запису агента чи підписки, які можна спробувати, констатувати цей факт; лабораторна робота не вимагає створювати чи купувати їх. Самостійно завершити задокументований ручний шлях до запланованої демонстрації. Під час тієї демонстрації викладач оцінює, чи обґрунтування задовольняє правило допуску запасного шляху; це рішення оцінювання не є кроком лабораторної роботи, що змінює стан. Детерміновані шлюзи та шлях прийнятого артефакту не змінюються.

На Windows типовим інсталятором є офіційний скрипт Google з областю користувача. Запустити його з PowerShell без прав адміністратора:

```powershell
irm https://antigravity.google/cli/install.ps1 | iex
```

Це офіційний скрипт інсталятора Google. Не замінювати його неофіційним завантаженням.

Закрити й знову відкрити PowerShell, потім перевірити встановлення та почати підтримуваний інтерактивний процес автентифікації:

```powershell
agy --help
agy models
```

Перша автентифікована взаємодія `agy` запускається, коли виконується `agy models`; завершити автентифікацію в браузері, коли її буде запрошено. Дані автентифікації належать студентові й не повинні копіюватися до репозиторію, `provision.log`, знімків екрана чи звіту.

Не використовувати `--dangerously-skip-permissions`. Під час пізнішого інтерактивного сеансу агента перевіряти кожну запрошену дію засобу й затверджувати лише читання навчального проєкту та редагування `reports/lab01/boundary-proposal.yaml`. Агент не повинен виконувати `decide`, виконувати `apply` або редагувати `student/design/learning-system-boundary.yaml`, незалежно від того, яке середовище взаємодії з ШІ-агентом використовується.

**Очікуваний результат:** на шляху агента обраний пропонувач може редагувати `reports/lab01/boundary-proposal.yaml`, тоді як його призначена роль виключає затвердження та застосування. Це розділення демонструється затвердженими діями засобів і збереженими свідченнями; воно не є межею дозволів операційної системи. На типовому шляху `agy --help` показує використання CLI, а `agy models` повертає моделі, доступні автентифікованому обліковому запису, не запитуючи окремо тарифікований ключ API. На іншій підписці `REPORT.md` називає те середовище взаємодії з ШІ-агентом, і `learning-project doctor` може зафіксувати Antigravity CLI як недоступний. На ручному запасному шляху студент зберігає потрібне обґрунтування та будь-які доступні очищені від чутливих даних свідчення відмови для `REPORT.md`; свідчення сеансу пропонувача не вигадуються.

### Крок 5: Ініціалізувати стан проєкту та сховища, що належать студентові

Перед створенням стану прочитати `requirements/SYSTEM_BRIEF.md`, `requirements/REQUIREMENTS_BASELINE.md` і `requirements/VAULT_STRUCTURE.md` із `training-project`. Визначити фіксовану мету системи, три межі стану, межу прийняття Лабораторної роботи 01 та мінімальну структуру сховища. Ці надані контракти визначають проєкт; цей крок не вимагає нових вимог чи архітектури застосунку.

Перейти до каталогу `training-project` клону. Створити каталоги проєктування та звіту, що належать студентові, і початковий файл звіту, потім скопіювати стартову пропозицію `upstream` до шляху звіту студента. Не редагувати `training-project/boundary-proposal.yaml` на місці.

```powershell
Set-Location "$HOME\projects\ai-systems-design-course\training-project"
New-Item -ItemType Directory -Force .\student\design | Out-Null
New-Item -ItemType Directory -Force .\reports\lab01\screenshots | Out-Null
Copy-Item .\boundary-proposal.yaml .\reports\lab01\boundary-proposal.yaml
if (-not (Test-Path .\reports\lab01\REPORT.md)) { New-Item -ItemType File .\reports\lab01\REPORT.md | Out-Null }
```

Створити нове зовнішнє сховище Markdown-файлів через інтерфейс Obsidian. Назвати його `ai-systems-learning-vault` або іншою нечутливою назвою та зберігати **поза** клоном Git. Додати один файл із назвою `README.md`, у якому зазначено, що сховище є канонічним навчальним станом під контролем студента і не є частиною репозиторію Git. Сховище має залишатися придатним до використання як звичайні файли без Obsidian. Obsidian може створити прихований каталог налаштувань `.obsidian/`; це метадані інтерфейсу, а не знання курсу, і не є артефактом подання.

Із `training-project` отримати точний коміт, що містить теорію, яку реєструють:

```powershell
git -C .. rev-parse HEAD
```

Усередині зовнішнього сховища створити `sources/module-01-ai-engineering-foundations.md`. Почати його з такої YAML-передмови та замінити заповнювач повним 40-символьним комітом із попередньої команди:

```yaml
---
record_type: source
source_id: module-01-ai-engineering-foundations
title: "Module 01: AI Engineering Foundations — Theory"
course_path: modules/01_AI_Engineering_Foundations/01_AI_Engineering_Foundations_Theory.md
course_commit: "<40-character Git commit>"
---
```

Цей запис вказує на точну теорію під контролем версій, не копіюючи її в друге місце, яке можна редагувати. Це реєстрація джерела, а не структурований запис концепції. Не створювати в сховищі каталоги `concepts/`, `questions/`, `proposals/`, `decisions/` або `operations/` під час Лабораторної роботи 01.

Захистити цей змінний канонічний стан від утрати робочої станції. На Windows найпростіша рекомендована базова лінія — створити сховище всередині каталогу, синхронізованого обліковим записом Microsoft OneDrive студента. Студент на Linux може використати наявне рівноцінне розташування синхронізації або резервного копіювання поза пристроєм. Не встановлювати й не проєктувати новий стек резервного копіювання для цієї лабораторної роботи. Якщо захист поза пристроєм недоступний, створити зовнішнє сховище та чесно зафіксувати це обмеження в `REPORT.md`.

Синхронізація поза пристроєм є початковим заходом захисту, а не повним проєктом аварійного відновлення: небажану зміну або вилучення також можна синхронізувати. Зберігання резервних копій, моніторинг і перевірка відновлення належать до роботи над системою в промисловій експлуатації в модулі 08.

Для цієї лабораторної роботи сховище доводить лише зовнішню межу системи та першу реєстрацію джерела. Особисті нотатки, облікові дані та повне особисте сховище не вимагаються і не подаються. Зафіксувати його розташування в `REPORT.md` як `external to repository`. Поданий знімок екрана може показувати вміст сховища, абсолютний шлях або обліковий запис операційної системи, коли це потрібно, щоб приписати його робочій станції, але не повинен показувати облікові дані, токени, секрети автентифікації чи сторонній приватний матеріал.

Лабораторна робота 01 не імпортує корпус концепцій і не будує стан пошуку, графа чи векторів. Ці можливості запроваджуються лише після того, як у пізніших модулях з’являться їхня теорія та контракти.

**Очікуваний результат:** у клоні існують `student/design/`, `reports/lab01/screenshots/` і `reports/lab01/REPORT.md`, `reports/lab01/boundary-proposal.yaml` існує як копія, а зовнішнє сховище Markdown-файлів містить нейтральний `README.md` і `sources/module-01-ai-engineering-foundations.md` як єдиний обов’язковий для курсу вміст Markdown. Запис джерела несе точний коміт клону та шлях курсу, сховище існує поза клоном і не з’являється в `git status`.

### Крок 6: Відтворити та протестувати середовище проєкту

Із `training-project` створити локальне середовище Python проєкту та запустити публічні тести прийняття:

```powershell
uv sync
uv run python -m unittest discover -s tests/public -v
uv run learning-project doctor --output .\reports\lab01\environment-report.json
```

Середовище під `.venv/` є похідним станом і ігнорується Git. Не додавати його до коміту.

Перед використанням робочого процесу ШІ довести, що пропозицію не можна прийняти без людського рішення:

```powershell
uv run learning-project validate .\reports\lab01\boundary-proposal.yaml
uv run learning-project apply .\reports\lab01\boundary-proposal.yaml --decision .\reports\lab01\boundary-decision.json --output .\student\design\learning-system-boundary.yaml
```

Стартовий файл структурно коректний, але містить навчальні заповнювачі. Перевірка має завершитися успішно, тоді як застосування має завершитися невдачею, бо рішення не існує. Команда `doctor` має записати нормалізований звіт про середовище й повернути зелений результат до кроку ШІ. Зелений означає підтримуваний хост Windows або Linux із доступними Git, GitHub CLI, `uv` та Obsidian. Автентифікований Antigravity CLI фіксується, коли використовується типовий пропонувач; він не потрібен для зеленого результату, коли використовується інше середовище взаємодії з ШІ-агентом. Підтвердити, що `student/design/learning-system-boundary.yaml` не було створено.

**Очікуваний результат:** усі публічні тести проходять; звіт про середовище фіксує зелений підтримуваний хост і чотири обов’язкові можливості набору інструментів; перевірка ідентифікує коректну пропозицію; передчасне застосування завершується з помилкою про відсутнє рішення і не створює прийнятого контракту.

### Крок 7: Попросити ШІ підготувати обмежену пропозицію

Із `training-project` переглянути `../modules/01_AI_Engineering_Foundations/01_AI_Engineering_Foundations_Theory.md`, особливо розділи про оцінювання доцільності застосування ШІ, повноваження людини, пороги корисності, ризики промислової експлуатації та простіші альтернативи. Потім прочитати локальні надані контракти `requirements/SYSTEM_BRIEF.md`, `requirements/REQUIREMENTS_BASELINE.md` і `requirements/VAULT_STRUCTURE.md`. Пропозиція має зберегти фіксовану навчальну систему знань, визначену цими контрактами. Обрати одну нечутливу академічну або професійну сферу лише як можливе пізніше розширення, зафіксоване в `personal_domain`; вона не повинна перевизначати систему чи дозволяти завантаження персональних даних.

Скористатися доступним агентом, коли це можливо. Запустити пропонувача інтерактивно з `training-project`. Типова команда така:

```powershell
agy
```

Студент, який використовує іншу підписку на агента, запускає те середовище взаємодії з ШІ-агентом замість цього. Дати те саме завдання. Не змінювати шлях пропозиції, обов’язкові поля чи пізніші команди `validate` / `decide` / `apply`.

Якщо крок 4 встановив, що жоден шлях агента не є придатним, відкрити `reports/lab01/boundary-proposal.yaml` у текстовому редакторі та виконати те саме завдання вручну: прочитати названу теорію та надані контракти, замінити лише значення-заповнювачі, зберегти кожне поле та `status: proposed` і не створювати рішення чи прийнятий контракт. Поля `governance` описують заплановану звичайну систему з допомогою ШІ: ШІ може пропонувати, детермінований код перевіряє, а студент вирішує. Вони не стверджують, що ШІ є автором цього кандидата запасного шляху. Зафіксувати `manual no-agent fallback`, спробуваний або недоступний шлях, дату, спостережене обмеження та будь-який доступний шлях очищених від чутливих даних свідчень відмови в `REPORT.md`. Не вигадувати пояснення ШІ, транскрипт чату, обліковий запис чи знімок екрана пропонувача.

Дати агентові таке завдання в інтерактивному сеансі:

> Прочитати локальне надане означення системи в `requirements/SYSTEM_BRIEF.md`, застосовні зобов’язання в `requirements/REQUIREMENTS_BASELINE.md`, межу сховища в `requirements/VAULT_STRUCTURE.md` і коментарі в `reports/lab01/boundary-proposal.yaml`. Замінити лише значення-заповнювачі пропозиції. Зберегти фіксовану навчальну систему знань, визначену наданими контрактами; використати обрану мною нечутливу академічну або професійну сферу лише в `personal_domain` як можливе пізніше обмежене розширення. Залишити `schema_version` незмінним, зберегти `status: proposed`, використати новий `proposal_id` малими літерами з дефісами та зберегти кожне поле. ШІ може пропонувати вміст, але не може затверджувати його, створювати рішення, виконувати `decide`, виконувати `apply` або редагувати `student/design/learning-system-boundary.yaml`. Перед редагуванням пояснити фіксований результат системи, нецілі, обов’язки ШІ, детермінованого робочого процесу та людини, умову корисності, істотний ризик, простішу базову лінію без ШІ, потрібні свідчення та залишкову невизначеність. Назвати, які надані локальні файли підтримують пропозицію.

На шляху агента відповісти на будь-яке запитання щодо сфери, не надаючи персональної чи конфіденційної інформації. Перевірити пояснення та запропоновану різницю перед затвердженням редагування одного файлу. Завершити сеанс агента після оновлення `reports/lab01/boundary-proposal.yaml`. На ручному запасному шляху перевірити завершений файл щодо того самого завдання перед продовженням.

**Очікуваний результат:** змінюється лише `reports/lab01/boundary-proposal.yaml`. Його `status` залишається `proposed`; він містить обмежений результат, щонайменше дві нецілі, явний розподіл між ШІ, детермінованим робочим процесом і повноваженнями людини, одну перевірювану умову корисності, один істотний ризик, одну простішу базову лінію без ШІ, щонайменше два пункти потрібних свідчень і одну чесну невизначеність. Шлях агента зберігає свідчення пропонувача. Ручний запасний шлях замість цього зберігає причину його використання і не претендує на авторство ШІ.

### Крок 8: Перевірити та розглянути пропозицію

Запустити детермінований засіб перевірки:

```powershell
uv run learning-project validate .\reports\lab01\boundary-proposal.yaml
git diff -- .\reports\lab01\boundary-proposal.yaml
```

Структурна коректність необхідна, але не встановлює семантичної якості. Розглянути пропозицію за такими запитаннями:

Якщо `validate` повідомляє будь-яку структурну помилку, не виконувати `decide`. Виправити лише пропозицію, зберігаючи `status: proposed`, потім повторно запустити `validate`. Починати семантичний розгляд лише після успішної перевірки.

1. Чи зберігає пропозиція надану навчальну систему знань, а `personal_domain` використовується лише для можливого пізнішого обмеженого розширення?
2. Чи описує запланований навчальний результат спостережувану здатність студента, а не «використати ШІ»?
3. Чи запобігають нецілі розширенню до повної системи в промисловій експлуатації або процесу рішень із високими наслідками?
4. Чи обмежує розподіл обов’язків ШІ пропонуванням, призначає структурне забезпечення детермінованому робочому процесу та зберігає повноваження затверджувати зміни за студентом? На ручному запасному шляху чи розрізняє звіт цей запланований звичайний розподіл і авторство студента щодо поточного кандидата?
5. Чи можна перевірити умову корисності на збережених свідченнях джерел?
6. Чи описує ризик правдоподібну відмову повного робочого процесу?
7. Чи є базова лінія без ШІ справді простішою і здатною закрити частину результату?
8. Чи охоплюють пункти потрібних свідчень як детерміновану поведінку, так і семантичний розгляд або розгляд обґрунтування джерелами?
9. Чи ідентифікує невизначеність те, що ще не встановлено?

Зафіксувати відповідь і висновок для кожного запитання в `reports/lab01/REPORT.md` до запуску `decide`.

Якщо будь-яка відповідь — ні, відхилити кандидата до його зміни:

```powershell
uv run learning-project decide .\reports\lab01\boundary-proposal.yaml --reject --by "<student-id>" --reason "<concise reason>" --decision .\reports\lab01\boundary-decision.json
```

Зберегти відхилену пропозицію та рішення в коміті Git, щоб відхилення залишалося придатним до розгляду. Потім переглянути пропозицію під новим `proposal_id`, вилучити лише згенерований файл рішення для заміненого розгляду командою `Remove-Item .\reports\lab01\boundary-decision.json` і повторити перевірку. Ніколи не редагувати зафіксоване рішення, щоб перетворити відхилення на затвердження. Якщо перший кандидат проходить семантичний розгляд, не створювати штучного відхилення лише щоб відпрацювати цей альтернативний шлях.

**Очікуваний результат:** остаточний кандидат проходить детерміновану перевірку, і студент може обґрунтувати кожну відповідь семантичного розгляду. Будь-яка відхилена спроба залишається пояснюваною з історії Git, а не мовчки перетворюється на затвердження.

### Крок 9: Зафіксувати людське затвердження та застосувати контракт

Лише після того, як пропозиція пройде обидва розгляди, зафіксувати рішення студента:

```powershell
uv run learning-project decide .\reports\lab01\boundary-proposal.yaml --approve --by "<student-id>" --decision .\reports\lab01\boundary-decision.json
uv run learning-project apply .\reports\lab01\boundary-proposal.yaml --decision .\reports\lab01\boundary-decision.json --output .\student\design\learning-system-boundary.yaml
```

Значення, передане через `--by`, є атрибуцією, наданою студентом. CLI проєкту вимагає непорожнього значення, але не автентифікує ідентичність оператора; звіт, подані свідчення Git і свідчення сеансу пропонувача, коли це застосовно, показують, що студент виконав команди, які несуть повноваження.

Переглянути рішення та прийнятий контракт:

```powershell
Get-Content .\reports\lab01\boundary-decision.json
Get-Content .\student\design\learning-system-boundary.yaml
```

**Очікуваний результат:** рішення фіксує `approved`, ідентифікатор пропозиції, дайджест вмісту SHA-256 точного вмісту пропозиції, ідентифікатор, наданий студентом, і час рішення. Прийнятий контракт має `status: approved` і розділ `accepted`, що пов’язує його з тим самим дайджестом вмісту пропозиції та зафіксованим затвердженням.

### Крок 10: Підготувати свідчення та зафіксувати результат комітом

Завершити файл `reports/lab01/REPORT.md`, створений на кроці 5. Пояснити власними словами студента:

- чому обраний `personal_domain` є лише можливим пізнішим розширенням і як запланований навчальний результат зберігає фіксовану навчальну систему знань;
- чому ШІ може бути корисним і що простіша базова лінія без ШІ вже здатна робити;
- чому звичайна система з допомогою ШІ надає ШІ повноваження пропонувати зміни, але не повноваження затверджувати зміни, і, на ручному запасному шляху, чому студент є автором цього кандидата, не змінюючи тієї запланованої межі системи;
- відповіді та висновок для всіх дев’яти запитань семантичного розгляду на кроці 8;
- один проєктний компроміс, що стосується корисності, ризику, вартості, затримки, приватності або супроводжуваності;
- чи збіглося друге застосування конфігурації робочої станції та будь-яку виявлену проблему;
- якого пропонувача використано, або що використано задокументований ручний запасний шлях без агента, спробуваний або недоступний шлях, дату та спостережений результат, а також будь-які доступні очищені від чутливих даних свідчення відмови доступу, служби або квоти;
- як відновлювалися б відтворюване середовище, артефакти проєкту під контролем Git і змінне сховище Markdown-файлів, і чи має сховище наразі копію поза пристроєм;
- чому успішний засіб перевірки схеми не доводить, що пропозиція є добрим проєктом системи.

Використати машинозчитуваний `reports/lab01/environment-report.json`, створений командою `learning-project doctor`. Не замінювати його рукописним списком пакунків і не редагувати червоний звіт у зелений. Зафіксувати окремі свідчення другого застосування, публічних тестів, передчасного застосування та прийнятого контракту в `REPORT.md` і знімках екрана.

Скопіювати транскрипт забезпечення до каталогу звіту після вилучення облікових даних, секретів автентифікації та стороннього приватного матеріалу:

```powershell
Copy-Item .\provision.log .\reports\lab01\provision.log
```

Не включати адреси електронної пошти, токени, секрети автентифікації, файли сеансів моделей або необроблені дампи змінних середовища. Абсолютний шлях або обліковий запис операційної системи можуть залишатися, коли це потрібно, щоб приписати свідчення робочій станції, але сторонні приватні шляхи та дані облікових записів необхідно вилучити. Зберегти вивід пакунків і збіжності в очищеному від чутливих даних транскрипті.

Кожен поданий знімок екрана має бути приписуваним робочій станції студента через видимий контекст операційної системи або відповідний підпис у `REPORT.md`. Зробити знімки екрана, які показують лише:

- `git remote -v` із `origin` на форку студента та `upstream` на `sobol-mo/ai-systems-design-course`;
- чотири встановлені пакунки робочої станції або успішні перевірки можливостей; збіжність засвідчує очищений від чутливих даних `provision.log`, а не знімок екрана;
- успішні публічні тести;
- на шляху агента — сеанс пропонувача без ідентифікаторів облікового запису служби агента, вмісту автентифікації та стороннього приватного матеріалу, який показує, що агент редагував лише `reports/lab01/boundary-proposal.yaml` і не виконував `decide` або `apply`; на ручному запасному шляху знімок екрана пропонувача не вигадується, тоді як будь-яка справжня помилка доступу, служби або квоти подається в очищеній від чутливих даних формі, а `REPORT.md` ідентифікує спробуваний або недоступний шлях, дату та спостережений результат;
- зовнішнє сховище Markdown-файлів, відкрите в Obsidian, із видимими нейтральним `README.md` і повною YAML-передмовою `sources/module-01-ai-engineering-foundations.md`; знімок екрана може показувати абсолютний шлях або обліковий запис операційної системи, щоб приписати сховище робочій станції, але не повинен показувати облікові дані чи секрети автентифікації;
- відхилене передчасне застосування та пізніший прийнятий контракт.

Переглянути стан репозиторію з `training-project`, потім зафіксувати комітом лише артефакти лабораторної роботи, що належать студентові. Не додавати до індексу `modules/`, `platform/`, `fixtures/`, `tests/public/`, `boundary-proposal.yaml` у корені проєкту, `.venv/` або `provision.log` у корені проєкту.

```powershell
git status --short
git diff --check
git add student/design/learning-system-boundary.yaml reports/lab01
git commit -m "feat(lab01): establish governed AI system boundary"
git status --short
git rev-parse HEAD
```

Автентифікувати GitHub CLI, потім надіслати особисту гілку до форку студента. Ціль надсилання — `origin`. Це не `upstream`.

```powershell
gh auth login
git push -u origin HEAD
```

**Очікуваний результат:** робоче дерево чисте після коміту, гілка існує у форку студента, `git remote -v` і надалі показує обов’язкову власність, а поданий коміт не містить правок, що належать `upstream`, `.venv/`, облікових даних, повного особистого сховища чи сторонніх даних робочої станції.

## Очищення та відкат

Установлені засоби робочої станції та зовнішнє сховище є наскрізними ресурсами курсу, тож успішна лабораторна робота їх не вилучає. Якщо робочий процес треба повторити до затвердження, відновити з `training-project` лише кандидата, що належить студентові, і згенеровані артефакти:

```powershell
Copy-Item .\boundary-proposal.yaml .\reports\lab01\boundary-proposal.yaml -Force
Remove-Item .\reports\lab01\boundary-decision.json -ErrorAction SilentlyContinue
Remove-Item .\student\design\learning-system-boundary.yaml -ErrorAction SilentlyContinue
```

Не використовувати ці команди після подання, якщо спочатку не збережено поданий коміт. Щоб повністю вийти із середовища курсу, вилучити локальний клон і зовнішнє навчальне сховище лише після збереження потрібної роботи, вилучити форк GitHub через налаштування репозиторію та виконати `gh auth logout` на спільній робочій станції. Вилучати встановлені пакунки або стан облікового запису агента лише через їхні офіційні засоби видалення або керування сеансом облікового запису; не вилучати каталоги пакунків або файли автентифікації вручну.

## Остаточна перевірка

Запустити повну детерміновану перевірку з `training-project`:

```powershell
uv run python -m unittest discover -s tests/public -v
uv run learning-project doctor --output .\reports\lab01\environment-report.json
uv run learning-project validate .\reports\lab01\boundary-proposal.yaml
uv run learning-project apply .\reports\lab01\boundary-proposal.yaml --decision .\reports\lab01\boundary-decision.json --output .\student\design\learning-system-boundary.yaml
Get-Content .\reports\lab01\environment-report.json -Raw | ConvertFrom-Json | Out-Null
git remote -v
git branch --show-current
git diff --check
git status --short
```

Послідовність самостійної роботи готова до запланованої демонстрації лише тоді, коли всі наведені далі умови спостережувані. Оцінювання викладачем і прийняття будь-якого обґрунтування ручного запасного шляху відбуваються під час тієї демонстрації; вони не є кроками лабораторної роботи, що змінюють стан і які студент має виконати заздалегідь:

- усі публічні тести проходять;
- пропозиція проходить перевірку;
- застосування без рішення раніше показано як невдале;
- остаточне рішення явно затверджує той самий `proposal_id` і дайджест вмісту SHA-256, що й пропозиція;
- прийнятий контракт фіксує затверджений результат і надану атрибуцію і може бути повторно згенерований із пропозиції та рішення; звіт і, на шляху агента, свідчення сеансу пропонувача приписують команди, що несуть повноваження, студентові;
- шлях агента містить справжні свідчення сеансу пропонувача, або `REPORT.md` містить обґрунтування ручного запасного шляху та доступні очищені від чутливих даних свідчення відмови для розгляду викладачем; запасний шлях не висуває претензії на авторство ШІ чи сеанс пропонувача і пояснює, що він не демонструє живого розділення акторів ШІ та студента;
- друге застосування конфігурації робочої станції збігається без ненавмисних перевстановлень, або друга перевірка можливостей на Linux повідомляє незмінені версії;
- `origin` є форком студента, `upstream` є `sobol-mo/ai-systems-design-course`, а особиста гілка не є `main`;
- зафіксовані комітом шляхи лише під `student/` і `reports/`;
- зовнішнє сховище Markdown-файлів містить нейтральний `README.md`, залишається поза Git, а його поточний захист поза пристроєм або відоме обмеження зазначено в `REPORT.md`;
- `sources/module-01-ai-engineering-foundations.md` у тому сховищі фіксує шлях теорії та повний коміт клону, тоді як сховище не містить структурованого виходу концепції, графового індексу чи векторного індексу;
- зафіксовані комітом і подані свідчення не містять облікових даних, токенів, секретів автентифікації чи стороннього приватного вмісту.

## Артефакти подання

Подати такі елементи окремо в Microsoft Teams, а не як архів:

- `reports/lab01/REPORT.md`;
- `reports/lab01/environment-report.json`;
- `reports/lab01/provision.log`;
- `reports/lab01/boundary-proposal.yaml`;
- `reports/lab01/boundary-decision.json`;
- `student/design/learning-system-boundary.yaml`;
- вибрані файли з `reports/lab01/screenshots/`;
- URL форку студента, назву гілки та точний хеш коміту.

Поданий коміт є станом реалізації, придатним до розгляду. Microsoft Teams є офіційним знімком із часовою позначкою. Знімки екрана та журнали доповнюють детерміновані перевірки; вони їх не замінюють.

## Контрольні запитання

1. Які свідчення в цій лабораторній роботі відрізняють інженерно спроєктований робочий процес системи ШІ від успішної демонстрації моделі?
2. Чому `boundary-proposal.yaml` не є прийнятим контрактом системи навіть після успішної структурної перевірки?
3. Який актор має повноваження пропонувати зміни, який актор має повноваження затверджувати зміни, які частини цього розрізнення забезпечує CLI проєкту, і які частини залишаються правилами врядування, що демонструються свідченнями?
4. Чому відхилене рішення не можна редагувати в затвердження?
5. Що робить умову корисності інформативнішою за твердження, що вихід «виглядає добре»?
6. Як базова лінія без ШІ допомагає вирішити, чи виправданий компонент ШІ?
7. Які свідчення робочої станції демонструють збіжність, а не лише успішне встановлення?
8. Які артефакти проєкту є канонічними, які є свідченнями аудиту, а які — похідним станом, який можна відбудувати?
9. Чому зовнішнє сховище Markdown-файлів зберігається поза репозиторієм Git і який механізм відновлення захищає його від утрати робочої станції?
10. Який проєктний компроміс зроблено в прийнятій межі і які свідчення могли б спричинити перегляд цього рішення?
11. У чому різниця між `origin` і `upstream` у цій лабораторній роботі і які каталоги студент може фіксувати комітом?
12. Чому студент може використати іншу підписку на агента або задокументований ручний запасний шлях, і який файл пропозиції та які детерміновані шлюзи мають залишатися тими самими?

## Необов’язкове розширення

Створити другу пропозицію для того самого запланованого навчального результату, у якій компонент ШІ вилучено. Порівняти два проєкти за корисністю, перевірюваністю, складністю, вартістю, затримкою та ризиком відмови. Не замінювати прийнятий контракт лабораторної роботи. Зберегти порівняння лише в `REPORT.md` і зазначити, які свідчення обґрунтовували б перехід від базової лінії без ШІ до проєкту з допомогою ШІ.
